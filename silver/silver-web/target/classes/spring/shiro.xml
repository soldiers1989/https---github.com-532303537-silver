<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://www.springframework.org/schema/beans" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.2.xsd  
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.2.xsd
		http://code.alibabatech.com/schema/dubbo
		http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 拦截器使用对象,使用Spring注入 -->

	<!-- <dubbo:reference id="sysUserProvider" interface="org.cloudx.provider.sys.SysUserProvider" 
		check="false" /> <dubbo:reference id="sysSessionProvider" interface="org.cloudx.provider.sys.SysSessionProvider" 
		check="false" /> <dubbo:reference id="sysAuthorizeProvider" interface="org.cloudx.provider.sys.SysAuthorizeProvider" 
		check="false" /> -->
	<bean id="realm" class="org.silver.shiro.Realm" /> 

	<!-- 配置Realm -->
	<bean id="userRealm" class="org.silver.shiro.UserRealm" />
	<bean id="adminRealm" class="org.silver.shiro.AdminRealm" />
 	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		
		<property name="authenticator" ref="authenticator"></property>
		<property name="realms">
			<list>
				<ref bean="userRealm" />
				<ref bean="adminRealm" />
			</list>
		</property>
	</bean>
<!--	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<property name="realm" ref="realm" />
	</bean> -->


	<!-- 配置使用自定义认证器，可以实现多Realm认证，并且可以指定特定Realm处理特定类型的验证 -->
	<bean id="authenticator" class="org.silver.shiro.CustomizedModularRealmAuthenticator">
		<!-- 配置认证策略，只要有一个Realm认证成功即可，并且返回所有认证成功信息 -->
		<property name="authenticationStrategy">
			<bean class="org.apache.shiro.authc.pam.AtLeastOneSuccessfulStrategy"></bean>
		</property>
	</bean>


	<!-- Web应用中,Shiro可控制的Web请求必须经过Shiro主过滤器的拦截,Shiro对基于Spring的Web应用提供了完美的支持 -->
	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<!-- Shiro的核心安全接口,这个属性是必须的 -->
		<property name="securityManager" ref="securityManager" />
		<!-- 要求登录时的链接 -->
		<property name="loginUrl" value="/unauthorized" />
		<!-- 用户访问未对其授权的资源时,所显示的连接 -->
		<property name="unauthorizedUrl" value="/forbidden" />
		<!-- Shiro连接约束配置,即过滤链的定义 -->
		<!-- anon：它对应的过滤器里面是空的,什么都没做 -->
		<!-- authc：该过滤器下的页面必须验证后才能访问,它是Shiro内置的一个拦截器org.apache.shiro.web.filter.authc.FormAuthenticationFilter -->
		<property name="filterChainDefinitions">
			<value>
				/servlet/validateCodeServlet=anon
				/regin=anon
				/login=anon
				/user/login=anon
				/notify_url.jsp=anon
				/tests/*=anon
				/Eport/*=anon
				/yspay/*=anon
				/account/home=anon
				/account/oauth=anon
				/oauth/token=anon
				/manage/manageLoginController/*=anon
				/logout=anon
				/test=anon
				/*.ico=anon
				/upload/*=anon
				/resource/*=anon
				/res/*=anon
				/unauthorized=anon
				/forbidden=anon
				/forgetPassword=anon
				/updatePassword=anon
				/checkAccount=anon
				/inspectPhone=anon
				/existencePhone=anon
				/sendOutPhone=anon
				/sendMessage=anon
				/sns/*=anon
				/*/api-docs=anon
				/callback*=anon
				/swagger*=anon
				/configuration/*=anon
				/*/configuration/*=anon
				/webjars/**=anon
				/**=authc,user
			</value>
		</property>
	</bean>

	<!-- 支持Shiro对Controller的方法级AOP安全控制 begin -->
	<bean
		class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator"
		depends-on="lifecycleBeanPostProcessor">
		<property name="proxyTargetClass" value="true" />
	</bean>
	<!-- Shiro生命周期处理器 -->
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor" />

</beans>